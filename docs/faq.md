# 常见问题

## 1. 为什么面板显示离线？
- 检查 `gate status` 确认服务是否运行。
- 检查 `gate test` 确认 API 是否能连通。
- 确认面板后台的**节点 ID** 和 **密钥** 与 `/etc/gate/gate.conf` 一致。

## 2. 如何在服务器上修改端口？
- **不要**在服务器修改。
- 请直接在面板后台修改节点端口，然后运行 `gate restart` 即可自动同步。

## 3. 支持哪些协议？
- 支持所有 sing-box 支持的协议，取决于面板下发的类型 (VMess, VLESS, Trojan 等)。
